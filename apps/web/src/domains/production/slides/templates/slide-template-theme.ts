/**
 * Structural color used by every reusable slide template.
 *
 * The generated deck owns its light/dark appearance. Templates only define
 * reusable layout, typography and accent decisions, so their base background
 * must stay stable across both appearances.
 */
export const SOFLIA_SLIDE_TEMPLATE_BACKGROUND = "#F7FAFC";
